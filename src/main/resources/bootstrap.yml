spring:
  profiles: dev
  cloud:
    config:
      uri: https://config.work.refinedev.io
  application:
    name: web_app
endpoints:
  refresh:
    enabled: true
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: refresh

---
spring:
  profiles: prd
  cloud:
    config:
      uri: https://config.work.refinehub.com
  application:
    name: web_app
endpoints:
  refresh:
    enabled: true
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: refresh