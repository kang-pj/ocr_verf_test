spring:
  application:
    name: rf-ocr-verf
  cloud:
    config:
      uri: http://localhost:8888  # Config Server URL
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.1